{
  "bank_version": "1.0.0",
  "last_updated": "2026-03-24",
  "total_questions": 58,
  "questions_per_session": 40,
  "estimated_session_minutes": 14,
  "difficulty_distribution": {
    "easy": { "count": 17, "pct": 29, "time_seconds": "18-20" },
    "medium": { "count": 24, "pct": 41, "time_seconds": 15 },
    "hard": { "count": 17, "pct": 29, "time_seconds": 12 }
  },
  "questions": [
    { "id": "Q01", "domain": "linux", "skill": "linux-users-permissions", "difficulty": "easy", "time_seconds": 20, "question": "A file has permissions `-rw-r-----`. Owner is `root`, group is `www-data`. User `deploy` is a member of `www-data`. Can `deploy` write to this file?", "options": [{ "id": "A", "text": "Yes — deploy is in the www-data group, and group permissions include write (rw-). Group members can read and write." }, { "id": "B", "text": "No — only the owner (root) has write permission. Group permissions are r-- (read only). deploy would need sudo." }, { "id": "C", "text": "Yes — any user in the www-data group inherits the owner's permissions, so deploy gets rw- access just like root." }, { "id": "D", "text": "No — the file has no execute permission, which is required before write operations can succeed on Linux." }], "correct": "B", "explanation": "Permissions: rw- (owner), r-- (group), --- (others). Group has read only." },
    { "id": "Q02", "domain": "linux", "skill": "linux-systemd", "difficulty": "easy", "time_seconds": 18, "question": "A service isn't starting after reboot. `systemctl status myapp` shows 'inactive (dead)'. Most likely reason?", "options": [{ "id": "A", "text": "The service crashed. Systemd permanently disabled it to prevent restart loops." }, { "id": "B", "text": "The unit file exists but `systemctl enable` was never run. Systemd only auto-starts enabled services at boot." }, { "id": "C", "text": "Server ran out of memory during boot. Systemd kills non-essential services when memory is low." }, { "id": "D", "text": "The service binary was deleted. Systemd detects missing binaries at boot and marks the service dead." }], "correct": "B", "explanation": "systemctl enable creates boot symlink. Without it, service exists but won't auto-start." },
    { "id": "Q03", "domain": "linux", "skill": "linux-filesystem", "difficulty": "medium", "time_seconds": 15, "question": "Root partition at 100%. You `rm` a 5GB log file, but `df -h` still shows 100%. Why?", "options": [{ "id": "A", "text": "rm only removes the directory entry. If a process still has the file open, disk space isn't freed until the file handle is released." }, { "id": "B", "text": "Linux caches deleted files in a recycle bin for 24 hours. Space freed after cache expires or running sync." }, { "id": "C", "text": "rm needs root to actually free space. Without sudo, it only removes the filename but preserves data." }, { "id": "D", "text": "The file was hardlinked elsewhere. rm only decrements link count; data persists until all hardlinks removed." }], "correct": "A", "explanation": "Open file descriptors prevent space reclamation. Use lsof to find the holding process." },
    { "id": "Q04", "domain": "linux", "skill": "linux-processes", "difficulty": "medium", "time_seconds": 15, "question": "Which approach most directly finds what process is listening on port 8080?", "options": [{ "id": "A", "text": "ps aux | grep 8080 — searches all processes for port 8080 in their arguments." }, { "id": "B", "text": "ss -tlnp | grep 8080 — shows TCP listening sockets with process ID and name per port." }, { "id": "C", "text": "lsof /var/run/*.8080 — checks Unix socket files associated with port 8080." }, { "id": "D", "text": "cat /etc/services | grep 8080 — maps port numbers to service names." }], "correct": "B", "explanation": "ss -tlnp shows listening TCP sockets with process info. Direct and reliable." },
    { "id": "Q05", "domain": "linux", "skill": "linux-processes", "difficulty": "hard", "time_seconds": 12, "question": "Process uses 80% RAM. You want OTHER processes to get CPU preference. Correct approach?", "options": [{ "id": "A", "text": "renice +10 -p PID — increases nice value, giving lower CPU priority. Other processes get more CPU time." }, { "id": "B", "text": "kill -SIGSTOP PID — freezes process, releasing memory pages to others while keeping it alive." }, { "id": "C", "text": "ulimit -m 50 — sets max resident memory to 50%, forcing kernel to swap excess." }, { "id": "D", "text": "cgroups memory.limit to cap at 50%. OOM killer reclaims excess and redistributes." }], "correct": "A", "explanation": "Question asks about CPU preference, not memory. renice adjusts CPU scheduling priority." },
    { "id": "Q06", "domain": "networking", "skill": "net-tcp-ip", "difficulty": "easy", "time_seconds": 18, "question": "https://example.com shows 'connection refused' but http works. What does 'connection refused' tell you?", "options": [{ "id": "A", "text": "SSL certificate is invalid. Server refused TLS handshake due to authentication failure." }, { "id": "B", "text": "Nothing is listening on port 443. Server's TCP stack rejected the connection — no process bound to that port." }, { "id": "C", "text": "DNS misconfiguration routing HTTPS to a different server without the application." }, { "id": "D", "text": "Browser HSTS policy blocking the connection. Site previously flagged as insecure." }], "correct": "B", "explanation": "Connection refused is TCP-level: SYN reached server but no process listening on port 443." },
    { "id": "Q07", "domain": "networking", "skill": "net-proxy", "difficulty": "medium", "time_seconds": 15, "question": "App behind Nginx reverse proxy. Logs show proxy IP instead of user IPs. Fix?", "options": [{ "id": "A", "text": "Configure Nginx to add X-Forwarded-For header. App reads client IP from that header instead of connection IP." }, { "id": "B", "text": "Remove the reverse proxy. Proxies always mask client IP with no workaround." }, { "id": "C", "text": "Enable transparent proxy mode in Nginx to forward TCP packets with original source IP." }, { "id": "D", "text": "App reads Host header for client IP. Nginx sets Host to original client's IP address." }], "correct": "A", "explanation": "X-Forwarded-For is the standard mechanism for preserving client IP through proxies." },
    { "id": "Q08", "domain": "networking", "skill": "net-dns", "difficulty": "medium", "time_seconds": 15, "question": "Can ping 8.8.8.8 but `ping google.com` fails with 'Name or service not known'. Issue?", "options": [{ "id": "A", "text": "Network interface only allows IP connections. Domain names need separate DNS interface." }, { "id": "B", "text": "DNS resolution broken. Server reaches internet (IP works) but can't resolve domains. Check /etc/resolv.conf." }, { "id": "C", "text": "Firewall blocks HTTP/HTTPS. Ping uses ICMP bypassing firewall, but DNS uses HTTP which is blocked." }, { "id": "D", "text": "Google DNS (8.8.8.8) responds to ICMP but blocks DNS queries from your IP range." }], "correct": "B", "explanation": "IP works = connectivity fine. Domain fails = DNS broken. Check /etc/resolv.conf for nameserver entries." },
    { "id": "Q09", "domain": "networking", "skill": "net-load-balancing", "difficulty": "hard", "time_seconds": 12, "question": "App behind ALB: works at low traffic, 502 errors under load. Health checks pass. Why?", "options": [{ "id": "A", "text": "ALB connection limit reached. Hard cap of 10K concurrent connections." }, { "id": "B", "text": "Backends responding too slowly under load. ALB timeout expires before response, returns 502." }, { "id": "C", "text": "ALB SSL certificate can't handle throughput. TLS handshakes become bottleneck." }, { "id": "D", "text": "AWS rate-limits security group rules under high traffic, throttling ALB-to-backend connections." }], "correct": "B", "explanation": "502 from ALB = backend invalid response or timeout. Under load, backends slow past ALB timeout." },
    { "id": "Q10", "domain": "git", "skill": "git-core", "difficulty": "easy", "time_seconds": 20, "question": "Committed and pushed a secret to GitHub. Why won't `git revert` + push make it safe?", "options": [{ "id": "A", "text": "git revert only works for merge commits. Need git reset --hard + force push." }, { "id": "B", "text": "Old commit with key still exists in Git history. Anyone can browse old commits. Must rewrite history AND rotate key." }, { "id": "C", "text": "GitHub caches reverted commits for 90 days on CDN." }, { "id": "D", "text": "git revert creates syntax error in commit chain. Push rejected by verification." }], "correct": "B", "explanation": "Git history preserves everything. Bots scan repos in seconds. Always rotate compromised credentials." },
    { "id": "Q11", "domain": "git", "skill": "git-collaboration", "difficulty": "medium", "time_seconds": 15, "question": "Two devs work on different files, push to same branch. Dev B's push rejected. Why?", "options": [{ "id": "A", "text": "Git locks branches during push. Dev B must wait for 5-minute lock expiry." }, { "id": "B", "text": "Dev B's local branch behind remote. Git rejects non-fast-forward pushes. Must pull first." }, { "id": "C", "text": "Git detected potential merge conflict. Blocks all pushes until resolved on server." }, { "id": "D", "text": "Branch protection blocks concurrent pushes within short time window." }], "correct": "B", "explanation": "Git requires fast-forward pushes. Remote has Dev A's commits that B doesn't have. Pull first." },
    { "id": "Q12", "domain": "git", "skill": "git-branching", "difficulty": "hard", "time_seconds": 12, "question": "Broken feature branch (50 commits) merged to main. Others pushed work after. Safest undo?", "options": [{ "id": "A", "text": "git revert -m 1 <merge-SHA> — single new commit undoing entire merge, preserves subsequent work." }, { "id": "B", "text": "git reset --hard to before merge + force push. Cleanest approach, won't affect later commits." }, { "id": "C", "text": "Cherry-pick all post-merge commits to new branch, delete and recreate main." }, { "id": "D", "text": "git bisect to find breaking commit, revert just that one to preserve useful work." }], "correct": "A", "explanation": "git revert -m 1 on merge commit creates reversal commit preserving all history. B destroys later work." },
    { "id": "Q13", "domain": "scripting", "skill": "script-bash", "difficulty": "easy", "time_seconds": 20, "question": "Script works with `bash script.sh` but 'Permission denied' with `./script.sh`. Why?", "options": [{ "id": "A", "text": "./ syntax only works for compiled binaries, not interpreted scripts." }, { "id": "B", "text": "File lacks execute permission. ./script.sh needs execute bit (chmod +x). bash script.sh only needs read." }, { "id": "C", "text": "Current directory . not in PATH. Shell can't find script without PATH entry." }, { "id": "D", "text": "Script has syntax error caught differently in ./ mode which uses stricter parsing." }], "correct": "B", "explanation": "./file executes directly (needs +x). bash file passes as argument (needs read only)." },
    { "id": "Q14", "domain": "scripting", "skill": "script-bash", "difficulty": "medium", "time_seconds": 15, "question": "Bash script works on Mac, garbled output on Linux. File created on Windows. What's happening?", "options": [{ "id": "A", "text": "Windows UTF-16 encoding vs Linux UTF-8 corrupts multi-byte characters." }, { "id": "B", "text": "Windows CRLF line endings (\\r\\n) vs Linux LF (\\n). Extra \\r treated as data, corrupting output." }, { "id": "C", "text": "Different Bash versions with incompatible syntax between Mac and Linux." }, { "id": "D", "text": "macOS Bash is Zsh-based with different file I/O. Script worked by accident on Mac." }], "correct": "B", "explanation": "Windows CRLF adds invisible \\r to every line, breaking string operations on Linux." },
    { "id": "Q15", "domain": "scripting", "skill": "script-bash", "difficulty": "medium", "time_seconds": 15, "question": "Script has `set -e`. Exits unexpectedly at `grep -q \"error\" logfile.log`. File exists, no match. Why?", "options": [{ "id": "A", "text": "set -e exits on any non-zero exit code. grep returns 1 when pattern not found, triggering exit." }, { "id": "B", "text": "set -e treats grep specially. Zero matches is fatal in strict mode." }, { "id": "C", "text": "-q flag suppresses output AND error handling. Without -q it would handle 'not found' correctly." }, { "id": "D", "text": "set -e pre-validates file operations. Detects pattern doesn't exist and exits to prevent false negatives." }], "correct": "A", "explanation": "set -e exits on non-zero return. grep returns 1 for no match. Fix: grep ... || true" },
    { "id": "Q16", "domain": "scripting", "skill": "script-python", "difficulty": "hard", "time_seconds": 12, "question": "Python script: 500 API calls, 1 sec each, 8 min total. Best way to speed up?", "options": [{ "id": "A", "text": "multiprocessing.Pool with 500 workers. Each call gets own OS process for true parallelism on CPU-bound work." }, { "id": "B", "text": "asyncio + aiohttp. API calls are I/O-bound. Async handles hundreds of concurrent requests in single thread." }, { "id": "C", "text": "threading with GIL disabled. Python 3.13+ GIL-free mode required for threads to make simultaneous calls." }, { "id": "D", "text": "subprocess.Popen for 500 separate Python processes. OS scheduler handles concurrency." }], "correct": "B", "explanation": "API calls are I/O-bound, not CPU-bound. asyncio is designed for concurrent I/O without thread/process overhead." },
    { "id": "Q17", "domain": "cloud", "skill": "cloud-networking", "difficulty": "easy", "time_seconds": 20, "question": "EC2 in public subnet, public IP, SG allows SSH from 0.0.0.0/0. Can't SSH. What to check?", "options": [{ "id": "A", "text": "Instance IAM role needs ec2:SSHAccess policy to accept connections." }, { "id": "B", "text": "Network ACL (NACL) might block port 22. NACLs are stateless, evaluated before security groups." }, { "id": "C", "text": "Instance in maintenance window. AWS blocks SSH during security patching." }, { "id": "D", "text": "Public IP expired. AWS recycles IPs every 24 hours without Elastic IP." }], "correct": "B", "explanation": "NACLs are stateless, evaluated before SGs. Need explicit inbound AND outbound allow rules." },
    { "id": "Q18", "domain": "cloud", "skill": "cloud-networking", "difficulty": "easy", "time_seconds": 18, "question": "Main difference between EC2 Security Group and Network ACL?", "options": [{ "id": "A", "text": "SGs are stateful (return traffic auto-allowed), instance-level. NACLs are stateless (explicit return rules needed), subnet-level." }, { "id": "B", "text": "SGs control inbound only. NACLs control both directions." }, { "id": "C", "text": "SGs are free, NACLs cost extra with deep packet inspection features." }, { "id": "D", "text": "SGs only support IP rules. NACLs support domain-based rules for blocking websites." }], "correct": "A", "explanation": "Stateful vs stateless, instance vs subnet. The fundamental AWS networking distinction." },
    { "id": "Q19", "domain": "cloud", "skill": "cloud-networking", "difficulty": "medium", "time_seconds": 15, "question": "App in private subnet needs internet (apt update). No public IP. What's needed?", "options": [{ "id": "A", "text": "Move to public subnet. Private subnets can never access internet." }, { "id": "B", "text": "NAT Gateway in public subnet. Private subnet route table points 0.0.0.0/0 to NAT. Outbound-only access." }, { "id": "C", "text": "Internet Gateway on VPC is sufficient. All subnets get internet with IGW." }, { "id": "D", "text": "VPN to office, route through office internet. Private subnets only reach internet via VPN." }], "correct": "B", "explanation": "NAT Gateway enables outbound internet from private subnets without making instances publicly reachable." },
    { "id": "Q20", "domain": "cloud", "skill": "cloud-storage", "difficulty": "medium", "time_seconds": 15, "question": "S3 bucket with versioning enabled. Accidentally delete important file. Recoverable?", "options": [{ "id": "A", "text": "No. S3 permanently removes on delete. Versioning only protects overwrites." }, { "id": "B", "text": "Yes. Versioning places 'delete marker' instead of removing data. Previous versions exist. Remove marker to restore." }, { "id": "C", "text": "Only within 24 hours. S3 keeps deleted files in temp buffer for one day." }, { "id": "D", "text": "Only with Object Lock. Standard versioning doesn't protect against deletions." }], "correct": "B", "explanation": "Versioning: delete adds marker, objects remain as previous versions. Remove marker to restore." },
    { "id": "Q21", "domain": "cloud", "skill": "cloud-managed-services", "difficulty": "hard", "time_seconds": 12, "question": "RDS PostgreSQL slow reads at peak. CPU at 40%. Most effective architectural fix?", "options": [{ "id": "A", "text": "Upgrade instance. At 40% CPU, approaching limit since RDS reserves 60% for internals." }, { "id": "B", "text": "Add read replica, route reads to it. 40% CPU = instance adequate, reads just need distribution." }, { "id": "C", "text": "Enable Multi-AZ. Standby handles read traffic during peak, doubling capacity." }, { "id": "D", "text": "Switch to Aurora Serverless for auto-scaling CPU and memory." }], "correct": "B", "explanation": "Read replicas handle read scaling. Multi-AZ standby does NOT serve reads (failover only)." },
    { "id": "Q22", "domain": "cloud", "skill": "cloud-compute", "difficulty": "hard", "time_seconds": 12, "question": "Lambda with 3+ second cold starts. Code is 5MB. Best architectural solution?", "options": [{ "id": "A", "text": "Provisioned concurrency keeps instances pre-initialized. AWS warms them, eliminating cold starts." }, { "id": "B", "text": "CloudWatch timer invokes every 5 min with dummy event. Keeps container warm." }, { "id": "C", "text": "Move to ECS Fargate. Containers run continuously, no cold starts." }, { "id": "D", "text": "Increase memory to 10GB. More CPU allocated proportionally speeds initialization." }], "correct": "A", "explanation": "Provisioned concurrency is AWS's official cold-start solution. B is unreliable hack. C changes architecture unnecessarily." },
    { "id": "Q23", "domain": "containers", "skill": "container-fundamentals", "difficulty": "easy", "time_seconds": 20, "question": "`docker ps` shows nothing. `docker ps -a` shows 5 'Exited' containers. What does this mean?", "options": [{ "id": "A", "text": "Containers crashed. Docker stopped them and marked 'Exited' for investigation." }, { "id": "B", "text": "docker ps shows running only. docker ps -a shows all including stopped. 'Exited' = main process ended, not necessarily error." }, { "id": "C", "text": "Docker in maintenance mode. Paused all for auto-update, will restart them." }, { "id": "D", "text": "Containers frozen, still using resources. Use docker unpause to resume." }], "correct": "B", "explanation": "docker ps = running. docker ps -a = all. Exited = process ended. Check exit code for error vs normal." },
    { "id": "Q24", "domain": "containers", "skill": "container-dockerfile", "difficulty": "easy", "time_seconds": 18, "question": "1.2GB Docker image for simple Python app. Single most impactful size reduction?", "options": [{ "id": "A", "text": "docker image prune after building removes unused layers, reducing 50-70%." }, { "id": "B", "text": "Switch base from python:3.11 to python:3.11-slim or alpine. Base image bloat is the #1 factor." }, { "id": "C", "text": "Add COMPRESS=true to Dockerfile. Built-in compression not enabled by default." }, { "id": "D", "text": "Combine all RUN commands. Each RUN creates a layer, and layer overhead causes large images." }], "correct": "B", "explanation": "python:3.11 ~900MB, python:3.11-slim ~150MB, alpine ~50MB. Base image is the biggest factor." },
    { "id": "Q25", "domain": "containers", "skill": "container-dockerfile", "difficulty": "medium", "time_seconds": 15, "question": "Change 1 line of code, rebuild. pip install reruns (3 min). How to avoid?", "options": [{ "id": "A", "text": "--cache-from flag pulls remote cache, skipping unchanged layers." }, { "id": "B", "text": "COPY requirements.txt and pip install BEFORE copying code. Cache is order-dependent — unchanged requirements = cached install." }, { "id": "C", "text": "Add pip install to .dockerignore to prevent cache invalidation." }, { "id": "D", "text": "docker buildx has smart dependency detection that skips pip install when only code changes." }], "correct": "B", "explanation": "Docker layer caching is order-dependent. Copy deps first, install, then copy code. Most important Dockerfile pattern." },
    { "id": "Q26", "domain": "containers", "skill": "container-fundamentals", "difficulty": "medium", "time_seconds": 15, "question": "Container works locally, 'exec format error' in production. Both Linux. Cause?", "options": [{ "id": "A", "text": "Built on ARM Mac (M1/M2/M3), production is AMD64. Architecture mismatch." }, { "id": "B", "text": "Production Docker version older, doesn't support Dockerfile syntax used." }, { "id": "C", "text": "Image corrupted during registry push. Re-push fixes it." }, { "id": "D", "text": "Production kernel too old. Container binaries need minimum kernel version." }], "correct": "A", "explanation": "exec format error = architecture mismatch. Very common with ARM Macs + x86 servers. Fix: --platform linux/amd64" },
    { "id": "Q27", "domain": "containers", "skill": "container-dockerfile", "difficulty": "hard", "time_seconds": 12, "question": "Go binary works in Ubuntu builder stage, crashes 'not found' in Alpine final stage. File exists. Why?", "options": [{ "id": "A", "text": "Alpine uses musl libc, Ubuntu uses glibc. Binary dynamically linked against glibc. Fix: CGO_ENABLED=0 for static binary." }, { "id": "B", "text": "Alpine doesn't support Go binaries. Go only compatible with glibc distributions." }, { "id": "C", "text": "File permissions lost during COPY. Alpine has stricter defaults needing explicit chmod." }, { "id": "D", "text": "Alpine is case-sensitive, Ubuntu isn't. Binary name casing mismatch." }], "correct": "A", "explanation": "Classic gotcha. CGO links against glibc dynamically. Alpine has musl. 'not found' = dynamic linker missing." },
    { "id": "Q28", "domain": "kubernetes", "skill": "k8s-core-objects", "difficulty": "easy", "time_seconds": 20, "question": "Difference between a Kubernetes Deployment and a Pod?", "options": [{ "id": "A", "text": "Pod = single container. Deployment = group of Pods behind load balancer." }, { "id": "B", "text": "Pod = smallest unit (1+ containers). Deployment manages Pods: ensures replica count, handles rolling updates, recreates failed Pods." }, { "id": "C", "text": "Pod = single node, for dev. Deployment = multi-node, for production." }, { "id": "D", "text": "Pod = stateless. Deployment = stateful with persistent storage." }], "correct": "B", "explanation": "Pod = smallest deployable unit. Deployment = controller managing pods via ReplicaSets." },
    { "id": "Q29", "domain": "kubernetes", "skill": "k8s-troubleshooting", "difficulty": "easy", "time_seconds": 18, "question": "Pod in ImagePullBackOff status. What does this mean?", "options": [{ "id": "A", "text": "Scheduler can't place pod on node. BackOff = retrying placement with delays." }, { "id": "B", "text": "Can't download container image. Wrong name/tag, registry auth needed, or registry unreachable. Retries with backoff." }, { "id": "C", "text": "Container started but crashed. Image pulled but backing off before restart." }, { "id": "D", "text": "Node lacks disk space for image. BackOff waiting for space." }], "correct": "B", "explanation": "ImagePullBackOff = image pull failed. Common: typo in name, missing imagePullSecrets, registry down. C describes CrashLoopBackOff." },
    { "id": "Q30", "domain": "kubernetes", "skill": "k8s-core-objects", "difficulty": "medium", "time_seconds": 15, "question": "Pod has memory limit 512Mi. Tries to use 600Mi. What happens?", "options": [{ "id": "A", "text": "Kubernetes throttles memory to 512Mi, like CPU throttling. Pod continues." }, { "id": "B", "text": "Container OOMKilled. Unlike CPU (throttled), memory limits are hard-enforced by killing the process." }, { "id": "C", "text": "Nothing for 5 min. Then evicted if still over limit." }, { "id": "D", "text": "Pod borrows unused memory from other pods on same node." }], "correct": "B", "explanation": "CPU = throttled (pod slows). Memory = hard kill (OOMKilled). Fundamental K8s distinction." },
    { "id": "Q31", "domain": "kubernetes", "skill": "k8s-networking", "difficulty": "medium", "time_seconds": 15, "question": "Service type ClusterIP. Can external traffic reach it?", "options": [{ "id": "A", "text": "Yes — ClusterIP describes IP assignment, not accessibility." }, { "id": "B", "text": "No — ClusterIP is internal only. Use NodePort, LoadBalancer, or Ingress for external access." }, { "id": "C", "text": "Yes, if cluster firewall allows. ClusterIP listens on all node interfaces." }, { "id": "D", "text": "No — ClusterIP deprecated for external. K8s 1.25+ requires ExternalDNS." }], "correct": "B", "explanation": "ClusterIP = internal only. External: NodePort, LoadBalancer, or Ingress." },
    { "id": "Q32", "domain": "kubernetes", "skill": "k8s-deployments", "difficulty": "medium", "time_seconds": 15, "question": "Deployment replicas:3 but kubectl shows 4 pods (3 Running + 1 Terminating). Problem?", "options": [{ "id": "A", "text": "Yes — two ReplicaSets competing, creating extras." }, { "id": "B", "text": "No — normal during rolling update or scale-down. New pods created before old terminated. Will resolve." }, { "id": "C", "text": "Yes — Terminating pod stuck. Force-delete needed." }, { "id": "D", "text": "No — K8s runs extra pod as hot standby for instant failover." }], "correct": "B", "explanation": "Terminating pod during rolling update is normal. Graceful shutdown before removal." },
    { "id": "Q33", "domain": "kubernetes", "skill": "k8s-core-objects", "difficulty": "hard", "time_seconds": 12, "question": "Updated ConfigMap used for pod env vars. Pods still show old values. Why?", "options": [{ "id": "A", "text": "ConfigMaps are immutable. Must delete and recreate, then restart pods." }, { "id": "B", "text": "Env vars from ConfigMaps set at startup, never auto-update. Must restart pods. Volume-mounted ConfigMaps DO update." }, { "id": "C", "text": "K8s caches ConfigMaps 5 min at node level. Wait for TTL." }, { "id": "D", "text": "Pod ServiceAccount lacks RBAC to read updated ConfigMap." }], "correct": "B", "explanation": "Env vars are static after pod creation. Volume mounts update eventually. Restart needed for env vars." },
    { "id": "Q34", "domain": "kubernetes", "skill": "k8s-troubleshooting", "difficulty": "hard", "time_seconds": 12, "question": "Pod has liveness AND readiness probes. Readiness probe fails. What happens?", "options": [{ "id": "A", "text": "Pod killed and restarted. Both probe failures trigger restart." }, { "id": "B", "text": "Pod keeps running but removed from Service endpoints (no traffic). Not restarted. Traffic resumes when readiness passes." }, { "id": "C", "text": "Pod enters CrashLoopBackOff with exponential backoff." }, { "id": "D", "text": "Nothing. Readiness probes are informational only." }], "correct": "B", "explanation": "Readiness = traffic routing. Liveness = lifecycle. Different purposes. Most commonly misunderstood K8s concept." },
    { "id": "Q35", "domain": "iac", "skill": "iac-terraform-core", "difficulty": "easy", "time_seconds": 20, "question": "terraform plan shows '1 to add'. You apply. Run plan again. What shows?", "options": [{ "id": "A", "text": "'1 to add' again. Terraform recreates on every apply. Need terraform import." }, { "id": "B", "text": "'No changes. Infrastructure is up-to-date.' State matches code after apply." }, { "id": "C", "text": "'1 to change' — cloud provider added defaults that differ from code." }, { "id": "D", "text": "'1 to destroy' — resources scheduled for cleanup without lifecycle block." }], "correct": "B", "explanation": "After apply, resource in state matches code. Plan finds no differences. Core Terraform loop." },
    { "id": "Q36", "domain": "iac", "skill": "iac-terraform-state", "difficulty": "medium", "time_seconds": 15, "question": "Two people run terraform apply simultaneously on same state. What happens?", "options": [{ "id": "A", "text": "Terraform auto-merges changes like Git conflict resolution." }, { "id": "B", "text": "Second silently overwrites first. No concurrent protection." }, { "id": "C", "text": "With remote state locking (S3+DynamoDB), second fails with lock error. Without locking, state corruption risk." }, { "id": "D", "text": "Both succeed, creating duplicates tracked in state." }], "correct": "C", "explanation": "State locking prevents concurrent applies. Without it, corruption risk. Why remote state + locking is best practice." },
    { "id": "Q37", "domain": "iac", "skill": "iac-terraform-core", "difficulty": "medium", "time_seconds": 15, "question": "Existing manual AWS infra. Write Terraform code, run apply. What happens?", "options": [{ "id": "A", "text": "Terraform detects existing resources, auto-maps to code." }, { "id": "B", "text": "Tries to create NEW duplicates. Doesn't know existing resources. Use terraform import first." }, { "id": "C", "text": "Error: resources exist. Must delete before Terraform manages them." }, { "id": "D", "text": "Overwrites existing with code config. Automatic replacement." }], "correct": "B", "explanation": "Terraform only knows resources in its state. Without import, treats code as new = creates duplicates." },
    { "id": "Q38", "domain": "iac", "skill": "iac-terraform-core", "difficulty": "hard", "time_seconds": 12, "question": "Module creates RDS + EC2. EC2 needs RDS endpoint in user data. How to pass it?", "options": [{ "id": "A", "text": "Hardcode endpoint. Both in same module, endpoint known at write time." }, { "id": "B", "text": "Reference: aws_db_instance.mydb.endpoint. Terraform resolves dependency — creates RDS first, passes endpoint to EC2." }, { "id": "C", "text": "Two separate applies: create RDS first, note endpoint, update EC2, apply again." }, { "id": "D", "text": "null_resource with local-exec to query AWS for endpoint after creation." }], "correct": "B", "explanation": "Terraform dependency graph handles this. Reference creates implicit dependency: RDS first, then EC2." },
    { "id": "Q39", "domain": "iac", "skill": "iac-terraform-state", "difficulty": "hard", "time_seconds": 12, "question": "Accidentally terraform destroy on production. Code and state intact. Can you recover?", "options": [{ "id": "A", "text": "terraform apply recreates infra from code. But DATA (DB records, files) permanently lost without separate backups." }, { "id": "B", "text": "terraform state undo restores previous state. Terraform keeps state history for rollback." }, { "id": "C", "text": "terraform recover restores resources. AWS preserves deleted resources 7 days." }, { "id": "D", "text": "terraform import with old IDs from state history. Resources still exist but detached." }], "correct": "A", "explanation": "Terraform recreates infrastructure (new IDs/IPs). DATA is gone forever without independent backups. IaC ≠ data backup." },
    { "id": "Q40", "domain": "cicd", "skill": "cicd-design", "difficulty": "easy", "time_seconds": 18, "question": "Primary purpose of a CI/CD pipeline?", "options": [{ "id": "A", "text": "Replace developers. Automates code writing so devs only review AI output." }, { "id": "B", "text": "Automate building, testing, deploying. Every change verified (CI) and optionally deployed (CD). Reduces errors, speeds delivery." }, { "id": "C", "text": "Backup code repository. Creates snapshots every commit for disaster recovery." }, { "id": "D", "text": "Monitor production. CI/CD = Continuous Inspection / Continuous Detection for error tracking." }], "correct": "B", "explanation": "CI = build + test automatically. CD = deploy automatically or with approval." },
    { "id": "Q41", "domain": "cicd", "skill": "cicd-testing", "difficulty": "medium", "time_seconds": 15, "question": "Tests pass locally, fail in CI with 'Connection refused' to test DB. Most likely cause?", "options": [{ "id": "A", "text": "CI sandboxed, can't access databases. Use SQLite for CI." }, { "id": "B", "text": "CI has no test DB running. Add database service container to CI config." }, { "id": "C", "text": "CI blocks all outbound connections for security." }, { "id": "D", "text": "Test DB credentials expired. CI uses separate creds needing rotation." }], "correct": "B", "explanation": "CI runs in fresh environment. Must explicitly provide services (DB, Redis, etc.) via service containers." },
    { "id": "Q42", "domain": "cicd", "skill": "cicd-design", "difficulty": "medium", "time_seconds": 15, "question": "CD auto-deploys on main merge. Friday 5PM merge introduces bug. Prevention?", "options": [{ "id": "A", "text": "Disable CI/CD on Fridays. Block merges after Thursday." }, { "id": "B", "text": "Manual approval gate before production. Auto-deploy to staging, human approves production." }, { "id": "C", "text": "Run tests twice (before and after merge). Both pass = guaranteed bug-free." }, { "id": "D", "text": "24-hour delay between merge and production deploy." }], "correct": "B", "explanation": "Manual approval for production is industry standard. Fast feedback to staging, human gate for prod." },
    { "id": "Q43", "domain": "cicd", "skill": "cicd-artifacts", "difficulty": "hard", "time_seconds": 12, "question": "CI takes 8 min. npm install reruns when deps unchanged. Most effective fix?", "options": [{ "id": "A", "text": "Pipeline caching (actions/cache) keyed on package-lock.json hash. Unchanged deps = skip install." }, { "id": "B", "text": "Nightly pipeline for npm install. Daytime builds use yesterday's deps." }, { "id": "C", "text": "Pre-install all packages in custom CI runner image." }, { "id": "D", "text": "Run npm install parallel with build. Node supports concurrent install + compile." }], "correct": "A", "explanation": "Cache keyed on lockfile hash. If unchanged, restore node_modules from cache. Standard CI optimization." },
    { "id": "Q44", "domain": "cicd", "skill": "cicd-design", "difficulty": "hard", "time_seconds": 12, "question": "Monorepo, 3 services. Every push builds all 3 even when 1 changed. How to optimize?", "options": [{ "id": "A", "text": "Split into 3 repos. Monorepos incompatible with efficient CI/CD." }, { "id": "B", "text": "Path-based triggers. Only build service when its directory files change (GitHub Actions paths filter)." }, { "id": "C", "text": "Run all 3 parallel. Total time = one build, no need to skip." }, { "id": "D", "text": "Cache build output by code hash. Matching hash = reuse artifact." }], "correct": "B", "explanation": "Path-based triggers = standard monorepo CI optimization. Build only what changed." },
    { "id": "Q45", "domain": "monitoring", "skill": "mon-metrics", "difficulty": "easy", "time_seconds": 18, "question": "Difference between monitoring and observability?", "options": [{ "id": "A", "text": "Same thing. Observability is modern marketing term for monitoring." }, { "id": "B", "text": "Monitoring = WHEN something is wrong (predefined alerts). Observability = WHY (metrics + logs + traces for unexpected issues)." }, { "id": "C", "text": "Monitoring for infrastructure, observability for applications. Separate tools." }, { "id": "D", "text": "Monitoring is real-time, observability is historical analysis over weeks/months." }], "correct": "B", "explanation": "Monitoring = known-unknowns. Observability = unknown-unknowns. Three pillars: metrics, logs, traces." },
    { "id": "Q46", "domain": "monitoring", "skill": "mon-alerting", "difficulty": "medium", "time_seconds": 15, "question": "Alert at 3AM: 'Memory 92% on node-3'. Been 90%+ for weeks. What's wrong with this alert?", "options": [{ "id": "A", "text": "Threshold too low. 92% normal. Set at 98%." }, { "id": "B", "text": "Lacks time-based conditions. Should alert on rate of change, not static threshold. Stable 90% is different from climbing to 90%." }, { "id": "C", "text": "Should only fire during business hours." }, { "id": "D", "text": "Prometheus miscounts. Linux page cache shows as used but is available." }], "correct": "B", "explanation": "Static thresholds without trend context = noise. Alert on rate of change or approaching OOM, not absolute values." },
    { "id": "Q47", "domain": "monitoring", "skill": "mon-metrics", "difficulty": "medium", "time_seconds": 15, "question": "http_requests_total counter with status label. Correct PromQL for error rate %?", "options": [{ "id": "A", "text": "count(http_requests_total{status=~'5..'})" }, { "id": "B", "text": "rate(http_requests_total{status=~'5..'}[5m]) / rate(http_requests_total[5m]) * 100" }, { "id": "C", "text": "http_requests_total{status=~'5..'} / http_requests_total * 100" }, { "id": "D", "text": "avg(http_requests_total{status=~'5..'})" }], "correct": "B", "explanation": "Must use rate() on counters (always increasing). Raw counter division (C) gives meaningless results." },
    { "id": "Q48", "domain": "monitoring", "skill": "mon-dashboards", "difficulty": "hard", "time_seconds": 12, "question": "Dashboard: p99 latency 2.5s, p50 is 50ms. What does this pattern tell you?", "options": [{ "id": "A", "text": "Metrics broken. 50x ratio indicates measurement or histogram bucket error." }, { "id": "B", "text": "Most requests fast (50ms), ~1% very slow (2.5s+). Specific condition causes slowness: cache miss, heavy query, GC pauses." }, { "id": "C", "text": "Service overloaded. p99/p50 ratio grows linearly with load. Halving traffic fixes it." }, { "id": "D", "text": "p99 includes network latency, p50 only server time. Difference = network round-trip." }], "correct": "B", "explanation": "Large p50/p99 gap = bimodal distribution. Most fast, tail is slow. Engineering task: find what causes the tail." },
    { "id": "Q49", "domain": "security", "skill": "sec-secrets", "difficulty": "easy", "time_seconds": 18, "question": "Dev commits AWS key to public GitHub, immediately deletes commit + force pushes. Secret safe?", "options": [{ "id": "A", "text": "Yes — force push rewrites history. Safe if nobody forked." }, { "id": "B", "text": "No — GitHub caches commits, bots scan in seconds, fetched copies exist. Rotate key immediately." }, { "id": "C", "text": "Yes — GitHub auto-detects and redacts AWS keys before visibility." }, { "id": "D", "text": "Depends on speed. Under 60s, CDN hasn't propagated." }], "correct": "B", "explanation": "Bots scan GitHub in real-time. Force-push doesn't delete caches/forks. Always rotate compromised credentials." },
    { "id": "Q50", "domain": "security", "skill": "sec-secrets", "difficulty": "medium", "time_seconds": 15, "question": "App stores user passwords. Correct approach?", "options": [{ "id": "A", "text": "Encrypt with AES-256, decrypt on login to compare. Military-grade encryption." }, { "id": "B", "text": "Hash with bcrypt/Argon2, unique salt per user. Hash input on login, compare hashes. Never store actual password." }, { "id": "C", "text": "Store in separate encrypted table. Compromised main DB won't expose passwords." }, { "id": "D", "text": "Hash with SHA-256. Fast and widely supported. Speed important for login UX." }], "correct": "B", "explanation": "Hashing (irreversible), slow algorithm (anti-brute-force), unique salt (anti-rainbow-table). AES is reversible. SHA-256 too fast." },
    { "id": "Q51", "domain": "security", "skill": "sec-compliance", "difficulty": "medium", "time_seconds": 15, "question": "Principle of least privilege applied to K8s ServiceAccount?", "options": [{ "id": "A", "text": "Start with cluster-admin, gradually reduce. Avoids permission errors during dev." }, { "id": "B", "text": "Each ServiceAccount gets only exact permissions needed. Pod reading ConfigMaps only gets get/list on ConfigMaps." }, { "id": "C", "text": "Minimize number of ServiceAccounts. All namespace pods share one." }, { "id": "D", "text": "Assign at cluster level for visibility. Easier to audit than namespace-scoped." }], "correct": "B", "explanation": "Minimum necessary permissions. If pod compromised, minimal SA limits blast radius." },
    { "id": "Q52", "domain": "security", "skill": "sec-supply-chain", "difficulty": "hard", "time_seconds": 12, "question": "CI pulls node:18 without digest. Security audit flags it. Why?", "options": [{ "id": "A", "text": "Undigested images can't be vulnerability-scanned. Scanners need digest for CVE matching." }, { "id": "B", "text": "node:18 tag is mutable — can point to different content over time. Hijacked tag = compromised builds. Digest pinning ensures exact verified image." }, { "id": "C", "text": "Docker Hub rate-limits undigested pulls. Digests bypass limits via direct CDN." }, { "id": "D", "text": "Undigested images can't use content-addressable caching, wasting bandwidth." }], "correct": "B", "explanation": "Tags are mutable pointers. Supply chain attacks exploit this. Digest = immutability = same verified image always." },
    { "id": "Q53", "domain": "sre", "skill": "sre-slos", "difficulty": "easy", "time_seconds": 18, "question": "What does SLO stand for and why does it matter?", "options": [{ "id": "A", "text": "System Load Optimizer — auto-tunes server performance based on traffic patterns." }, { "id": "B", "text": "Service Level Objective — internal reliability target (e.g. 99.9% success). Creates error budget for acceptable failures." }, { "id": "C", "text": "Scheduled Latency Observation — monitoring schedule checking latency at intervals." }, { "id": "D", "text": "Software Lifecycle Overview — document describing product lifecycle." }], "correct": "B", "explanation": "SLO = reliability target. Error budget = 100% - SLO. Balances reliability vs development speed." },
    { "id": "Q54", "domain": "sre", "skill": "sre-incident", "difficulty": "medium", "time_seconds": 15, "question": "During incident, engineer starts reading code. Senior SRE says 'stop — mitigate first.' Why?", "options": [{ "id": "A", "text": "Reading code during incidents is a security risk." }, { "id": "B", "text": "Mitigation (rollback, scale up) restores service immediately. Root cause analysis after. 30 min debugging while users down > 2 min rollback." }, { "id": "C", "text": "Only original developer should read code. SRE monitors dashboards." }, { "id": "D", "text": "Code fixes need PR review, too slow during incident." }], "correct": "B", "explanation": "Mitigate first, investigate second. Restore service (reduce MTTR), then postmortem for root cause." },
    { "id": "Q55", "domain": "sre", "skill": "sre-slos", "difficulty": "hard", "time_seconds": 12, "question": "99.9% SLO, currently at 99.85%. Product wants major feature deploy. What should happen?", "options": [{ "id": "A", "text": "Deploy anyway. SLO is guideline. Features = revenue." }, { "id": "B", "text": "Error budget exhausted (99.85% < 99.9%). Freeze features, focus reliability until budget recovers." }, { "id": "C", "text": "Split feature into smaller deploys. Smaller = always safer." }, { "id": "D", "text": "Deploy with 50% traffic. Halves SLO impact." }], "correct": "B", "explanation": "Core SRE: burned error budget = reliability > features. Self-correcting system. Without discipline, SLOs are meaningless." },
    { "id": "Q56", "domain": "finops", "skill": "finops-awareness", "difficulty": "easy", "time_seconds": 18, "question": "EC2 bill $3K/month. Instances run 24/7, used only business hours. Simplest optimization?", "options": [{ "id": "A", "text": "Migrate to Lambda. Serverless charges only for compute time." }, { "id": "B", "text": "Schedule stop outside business hours. 168 hrs/week → ~50 hrs/week = ~70% savings." }, { "id": "C", "text": "Spot instances. 90% cheaper, fine for interruptible workloads." }, { "id": "D", "text": "3-year reserved instances. 60% savings from commitment." }], "correct": "B", "explanation": "Instance scheduling = simplest, lowest-risk win. Stop when unused. ~70% savings immediately." },
    { "id": "Q57", "domain": "finops", "skill": "finops-rightsizing", "difficulty": "medium", "time_seconds": 15, "question": "$800/month data transfer. EC2 to S3, same region (should be free). Where's the cost?", "options": [{ "id": "A", "text": "Same-region S3 transfers aren't free. Common misconception." }, { "id": "B", "text": "Cross-AZ transfer. EC2 and S3 in different AZs charge per GB." }, { "id": "C", "text": "Traffic routes through NAT Gateway ($0.045/GB processing). Use S3 VPC Gateway Endpoint (free) instead." }, { "id": "D", "text": "S3 API request costs accumulated at high request volume." }], "correct": "C", "explanation": "NAT Gateway processing fees on S3 traffic = common hidden cost. S3 VPC Gateway Endpoint is free, eliminates NAT." },
    { "id": "Q58", "domain": "finops", "skill": "finops-rightsizing", "difficulty": "hard", "time_seconds": 12, "question": "RDS db.r5.2xlarge, $1,200/month. Avg CPU 15%, memory 30%. Should you downsize?", "options": [{ "id": "A", "text": "Yes, immediately to db.r5.large. 15% CPU = massively over-provisioned." }, { "id": "B", "text": "Check PEAK utilization first. Averages misleading — peak might hit 70%+. Also check IOPS, connections, query latency." }, { "id": "C", "text": "No — RDS needs 70%+ headroom for Multi-AZ failover reconnection storms." }, { "id": "D", "text": "Switch to Aurora Serverless. Auto-scales, always cheaper." }], "correct": "B", "explanation": "Averages hide peaks. 15% average might = 80% peak. Check peak CPU, IOPS, connections before downsizing." }
  ]
}
